/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { hostname: "wvdtjupxivp8nd8d.public.blob.vercel-storage.com" },
    ],
  },
};

module.exports = nextConfig;
